<?xml version="1.0" encoding="utf-8"?>
<LinearLayout 
    xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="fill_parent"
    android:layout_height="fill_parent"
    android:orientation="vertical" android:padding="50dp">

    <Button
        android:id="@+id/bin_commands"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:text="@string/bin_commands" 
        android:layout_marginBottom="25dp" 
        android:height="75dp" 
        android:onClick="onBinCommandsButtonClick" />

    <Button
        android:id="@+id/network_adapters"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:text="@string/network_adapters" 
        android:height="75dp"
        android:onClick="onNetworkAdaptersButtonClick" />
    
</LinearLayout>